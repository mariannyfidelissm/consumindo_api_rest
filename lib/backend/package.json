{
  "name": "backend_example",
  "version": "1.0.0",
  "description": "Exemplo de API simples com NodeJs",
  "main": "index.js",
  "scripts": {
    "dev": "node src/server.js",
    "reset-db": "npx knex migrate:rollback && npx knex migrate:latest && npx knex seed:run",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Marianny Fidelis",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "knex": "^2.3.0",
    "sqlite3": "^5.1.2"
  }
}
